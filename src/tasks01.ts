// Exercício 1
function calcularArea(largura: number, altura: number) {
    return largura * altura;
}
let largura: number = 10;
let altura: number = 23;

console.log(calcularArea(largura, altura)+ 'cm²');

